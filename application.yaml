# Do NOT edit this file directly.
# File generated from application.yaml.tmpl by the update-gitops-repo tool with arguments: /usr/bin/update-gitops-repo -git-revision=9696c245969df0bc059f06b3d40e1ab32d604dca -base-branch=jenkins-demo -create-pull-request -merge-pull-request -substitute=imageName=chhsiao/devx-dispatch-demo@sha256:13b774aa8effbc654d03ab6b28ae533c6b63c45bbac892d1f094a15af08d292b

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world
spec:
  replicas: 5
  selector:
    matchLabels:
      app: hello-world
  template:
    metadata:
      labels:
        app: hello-world
    spec:
      containers:
      - image: "chhsiao/devx-dispatch-demo@sha256:13b774aa8effbc654d03ab6b28ae533c6b63c45bbac892d1f094a15af08d292b"
        name: hello-world
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: hello-world
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: hello-world
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    certmanager.k8s.io/issuer: letsencrypt-issuer
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/auth-response-headers: X-Forwarded-User
    traefik.ingress.kubernetes.io/auth-type: forward
    traefik.ingress.kubernetes.io/auth-url: http://traefik-forward-auth-kubeaddons.kubeaddons.svc.cluster.local:4181/
    traefik.ingress.kubernetes.io/rule-type: PathPrefixStrip
  generation: 1
  labels:
    app.kubernetes.io/instance: hello-world
    app.kubernetes.io/managed-by: Tiller
    app.kubernetes.io/name: hello-world
  name: hello-world
spec:
  rules:
  - http:
      paths:
      - backend:
          serviceName: hello-world
          servicePort: 80
        path: /hello-world
---
apiVersion: batch/v1
kind: Job
metadata:
  generateName: trigger-jenkins-pipeline
  annotations:
    argocd.argoproj.io/hook: PostSync
    #argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  template:
    spec:
      containers:
      - name: curl
        image: curlimages/curl
        args:
        - -s
        - -u
        - $(USERNAME):$(PASSWORD)
        - -X
        - POST
        - -I
        - https://jenkins.mesosphere.com/service/jenkins/job/dispatch-remote-test-pipeline/build
        env:
        - name: USERNAME
          valueFrom:
            secretKeyRef:
              name: jenkins-secret
              key: username
        - name: PASSWORD
          valueFrom:
            secretKeyRef:
              name: jenkins-secret
              key: password
      restartPolicy: Never
